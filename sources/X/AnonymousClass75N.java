package X;

import android.text.Spannable;
import android.text.style.CharacterStyle;

/* renamed from: X.75N  reason: invalid class name */
public class AnonymousClass75N {
    public static void A00(Spannable spannable, Spannable spannable2, CharacterStyle characterStyle, CharacterStyle characterStyle2) {
        spannable.setSpan(characterStyle2, spannable2.getSpanStart(characterStyle), spannable2.getSpanEnd(characterStyle), spannable2.getSpanFlags(characterStyle));
    }
}
